# React comments app

simple chat app as described in the [react tutorial](https://facebook.github.io/react/docs/tutorial.html)

Get it working by loading the super simple server in python

1. pip install -r requirements.txt
2. python server.py
3. load localhost:3000
